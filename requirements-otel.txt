# OpenTelemetry export (optional dependency group)
#
# Candlewise's own observability (log_context / agent_runs / progress) doesn't need these packages;
# install them only to export traces to a standard APM (Jaeger / Tempo / Langfuse OTLP, etc.):
#
#   pip install -r requirements-otel.txt
#
# Then set the OTEL_EXPORTER_OTLP_ENDPOINT environment variable to enable it (see "OTel export" in the README).
# Without the packages or an endpoint, src/platform/observability/otel.py is a no-op with no side effects.

opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0
